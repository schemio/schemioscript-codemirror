# Simple expression

a1 +3

==>

Program(BinaryExpression(VariableName,Number))


# Simple expression2

(a1+(2-3))

==>

Program(
    ParenthesizedExpression(
        BinaryExpression(
            VariableName,ParenthesizedExpression(
                BinaryExpression(Number,Number)
            )
        )
    )
)


# Simple expression with whitespace

(a + b * 3)

==>

Program(BinaryExpression(VariableName,Number))
